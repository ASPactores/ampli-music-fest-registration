service: sari-sari-backend
frameworkVersion: "4"

custom:
  stage: ${opt:stage, 'development'}
  pythonRequirements:
    fileName: requirements.txt
    dockerizePip: linux
    no-deploy:
      - requests
      - boto3
      - botocore
    layer:
      name: sari-sari-backend-${self:custom.stage}-python-requirements
      compatibleRuntimes:
        - python3.10
    useDownloadCache: false
    useStaticCache: false
    slim: true
    slimPatternsAppendDefaults: false
    slimPatterns:
      - "/*.py[c|o]"
      - "/pycache*"

provider:
  name: aws
  runtime: python3.10
  region: ap-southeast-1
  profile: sari-sari-backend
  timeout: 30
  iamRoleStatements:
    - Effect: Allow
      Action:
        - ssm:GetParameter
      Resource: arn:aws:ssm:${self:provider.region}:${aws:accountId}:parameter/sari-sari-backend/*
  environment:
    ENV: ${self:custom.stage}
    SUPABASE_URL: ${ssm:/sari-sari-backend/supabase-url-${self:custom.stage}}
    SUPABASE_ANON_KEY: ${ssm:/sari-sari-backend/supabase-key-${self:custom.stage}}
    DATABASE_CONNECTION_STRING: ${ssm:/sari-sari-backend/database-connection-string-${self:custom.stage}}
    FRONTEND_URL: ${ssm:/sari-sari-backend/frontend-url-${self:custom.stage}}

functions:
  app:
    name: ${self:service}-${self:custom.stage}-app
    handler: main.handler
    layers:
      - Ref: PythonRequirementsLambdaLayer
    events:
      - http:
          path: /
          method: get
          cors: true
      - http:
          path: /{proxy+}
          method: any
          cors:
            origin: ${ssm:/sari-sari-backend/frontend-url-${self:custom.stage}}
            headers:
              - Content-Type
              - X-Amz-Date
              - Authorization
              - X-Api-Key
              - X-Amz-Security-Token
              - X-Forwarded-For
              - X-Forwarded-Port
              - Accept
              - Origin
              - Referer
              - User-Agent
            allowCredentials: true

package:
  individually: false
  include:
    - main.py
    - auth/**
    - database/**
    - middlewares/**
    - models/**
    - routes/**
    - utils/**

  exclude:
    - requirements.txt
    - "**/__pycache__/**"
    - "**/*.pyc"
    - .venv/**
    - node_modules/**
    - uv.lock
    - .DS_Store
    - .serverless/**
    - .idea/**
    - venv/**
    - package.json
    - package-lock.json
    - Pipfile
    - Pipfile.lock
    - log.txt
    - README.md
    - ruff.toml
    - scripts/**
    - resources/**
    - .env.*

plugins:
  - serverless-python-requirements
